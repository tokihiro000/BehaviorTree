public enum NodeState
{
    None,
    Enable,
    Running,
    Disable
}

public enum NodeType
{
    Selector,
    Sequencer,
    Action
}

public enum ActionState
{
    None,
    Start,
    Running,
    Finished
}
